name: form_inputs
description: Exposes reusable Formz inputs and validation rules
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  formz: ^0.7.0

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.24.4
  very_good_analysis: ^5.0.0+1
